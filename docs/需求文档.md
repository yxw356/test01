# 企业知识中枢 — 需求文档

> 版本：v1.4  
> 更新日期：2026-06-03  
> 说明：本文档描述系统功能需求、非功能需求、部署约束与验收标准；**第 11 章**为生产上线专用需求（基于 2026-06-03 项目评估与现网约束整理）。

---

## 1. 项目目标

建设面向企业的**私有知识库 RAG 智能问答平台**，实现：

- 企业知识文件（制度、流程、项目资料、方案、培训材料等）的统一上传与管理；
- 按用户、组织标签、公开范围进行**权限隔离**的检索与问答；
- 基于**混合检索（BM25 + 向量）+ RAG** 的可追溯问答，答案须来源于企业知识库内容；
- 支持多轮对话、流式输出与管理端运维能力。

---

## 2. 用户角色

| 角色 | 说明 | 主要能力 |
| --- | --- | --- |
| 普通用户（USER） | 企业员工 | 上传/管理本人知识文件、混合检索、智能问答、个人中心 |
| 管理员（ADMIN） | 系统管理员 | 普通用户全部能力 + 用户管理、组织标签、聊天记录查看 |

默认管理员：`admin` / `admin123`（首次启动自动初始化，**仅限开发/演示；生产环境禁止使用该默认账号**，见 [§11](#11-生产环境需求v12)）。

---

## 3. 功能需求

### 3.1 认证与权限

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| FR-AUTH-01 | 用户名密码登录，签发 JWT，支持 Token 刷新与注销 | P0 |
| FR-AUTH-02 | 接口按角色鉴权；未登录用户不可访问业务 API | P0 |
| FR-AUTH-03 | 知识检索与问答仅返回当前用户**有权限**的文档块（本人 / 公开 / 所属组织及层级标签） | P0 |
| FR-AUTH-04 | 组织标签支持层级关系；用户有效标签含默认组织 `DEFAULT` | P1 |

### 3.2 知识库文件管理

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| FR-KB-01 | 支持大文件**分片上传**、合并、断点续传、上传状态查询 | P0 |
| FR-KB-02 | 文件元数据写入 MySQL；合并后对象存储至 **MinIO**（桶名 `uploads`） | P0 |
| FR-KB-03 | 合并成功后通过 **Kafka** 异步触发：下载 → Tika 解析 → 切分 → 向量化 → 写入 ES | P0 |
| FR-KB-04 | 支持 PDF / Word / TXT 等（Tika）；表格 **.xlsx / .xls / .csv**（`TabularParseService`） | P0 |
| FR-KB-05 | 父子块切分：子块用于检索，父块用于聚合展示上下文 | P1 |
| FR-KB-06 | 文件列表按权限过滤；支持删除（MinIO + MySQL + ES 联动清理） | P1 |
| FR-KB-07 | 上传时可设置**是否公开**、**组织标签**（如 `default`） | P0 |

### 3.3 检索

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| FR-SEARCH-01 | 提供混合检索 API：`GET /api/v1/search/hybrid?query=&topK=` | P0 |
| FR-SEARCH-02 | 单路召回包含：原始 query 的 BM25 + 向量 KNN，RRF 融合后按父块聚合 | P0 |
| FR-SEARCH-03 | 知识库弹窗内可测试混合检索（前端代理 `/proxy-api`） | P1 |
| FR-SEARCH-04 | 向量维度与 Embedding 模型、ES `dense_vector.dims` **必须一致**（当前为 **1024 / bge-m3**） | P0 |

### 3.4 智能问答（RAG）

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| FR-CHAT-01 | WebSocket 流式问答；基于检索上下文 + 系统 Prompt 规则生成回答 | P0 |
| FR-CHAT-02 | 多轮对话：Redis 维护 `conversationId` 与近期历史 | P0 |
| FR-CHAT-03 | RAG 流水线：Query 理解 → 多路召回 → 候选过滤（RRF / Cross-Encoder 占位 / MMR）→ 可选 Reflection → 最终 Top-K | P1 |
| FR-CHAT-04 | Query 改写、HyDE、Reflection 可通过配置开关；**本地大模型环境建议关闭**以降低延迟 | P1 |
| FR-CHAT-05 | 无检索结果时，模型应说明「暂无相关信息」，不得编造企业事实 | P0 |

### 3.5 管理功能

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| FR-ADMIN-01 | 用户管理：列表、创建、禁用、组织标签分配 | P1 |
| FR-ADMIN-02 | 组织标签管理 | P1 |
| FR-ADMIN-03 | 聊天记录查询（管理端） | P2 |

### 3.6 知识库分域（试点）

> 配置与运营细则见 [知识库分域与边界定义.md](./知识库分域与边界定义.md)。现网以 **组织标签 `orgTag`** 映射业务库，单 ES 索引 `knowledge_base` 逻辑分区。

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| FR-KB-08 | 支持按业务域定义组织标签（如 `KB_POLICY`、`KB_PROJECT`、`KB_PRESALES`、`KB_OPS`），上传时必选标签入库 | P0（配置） |
| FR-KB-09 | 检索与问答仅返回用户 `org_tags` 有权限的文档（含公开文档规则）；跨库越权评测集 **0 次** | P0 |
| FR-KB-10 | 试点阶段先上线 **1～2** 个业务库，每库 100～300 份文档、20～30 题评测集（见 [eval/README.md](./eval/README.md)） | P1 |
| FR-KB-11 | 分库 Prompt 要点在运营文档中定义；全局 `ai.prompt.rules` 仍生效 | P1（文档） |

**试点约束：** 用户拥有多个 `org_tags` 时，问答检索为**并集**；要严格单库问答需控制用户标签数量，或依赖 §3.7 问答选库。

### 3.7 多知识库产品化（阶段 2）

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| FR-PROD-KB-01 | 新增 `knowledge_bases` 元数据（名称、`tag_ids`、`prompt_key`、状态）及管理端 CRUD | P2 |
| FR-PROD-KB-02 | ES 文档增加 `knowledge_base_id` 或按库分索引/别名，与 NFR-PROD-12 对齐 | P2 |
| FR-PROD-KB-03 | 智能问答支持**会话级选库**，检索 filter 仅当前 `kbId` | P2 |
| FR-PROD-KB-04 | 按库配置系统 Prompt（yml 或管理端），`RagPipeline` 按会话选择 rules | P2 |
| FR-PROD-KB-05 | 按库评测：读取 `docs/eval/{kb}/questions.jsonl`，发版前输出召回/拒答/越权报告（脚本或 CI） | P2 |
| FR-PROD-KB-06 | 知识库文件列表支持按 `orgTag` / `kbId` 筛选；上传默认标签可按角色限制 | P1 |

---

## 4. 非功能需求

| 编号 | 类别 | 需求 |
| --- | --- | --- |
| NFR-01 | 安全 | 内部资料不出域；JWT 密钥可配置；敏感配置不入库 |
| NFR-02 | 可用性 | 上传与索引异步解耦；索引失败可在日志中定位并重试（Kafka 重试 / 重启后 ES 维度修复自动重建） |
| NFR-03 | 性能 | 混合检索（关闭 Query 改写/HyDE）目标在秒级；完整 RAG Stage1 开启 LLM 增强时允许分钟级（受本地 GPU 约束） |
| NFR-04 | 可维护性 | ES 索引启动时校验向量维度；与 `embedding.api.dimension` 不一致时自动删索引重建并从 MySQL 分块重灌 |
| NFR-05 | 兼容性 | LLM / Embedding 均通过 **OpenAI 兼容 HTTP API** 接入（Ollama、vLLM 等） |

---

## 5. 外部依赖与接口

### 5.1 基础设施（Docker Compose）

| 组件 | 默认端口 | 用途 |
| --- | --- | --- |
| MySQL | 3307 | 用户、文件元数据、分块文本 |
| Redis | 6379 | 会话、Token 缓存 |
| MinIO | 19000 / 19001 | 对象存储；**须存在桶 `uploads`**（应用启动可自动创建） |
| Kafka | 9092 | 文件处理任务队列 |
| Elasticsearch | 9200 | 知识索引 `knowledge_base` |

### 5.2 模型服务（推荐本地 vLLM 双实例）

| 能力 | 默认配置 | 说明 |
| --- | --- | --- |
| 对话 LLM | `http://127.0.0.1:8000/v1`，模型如 `Qwen3.6-35B-A3B` | `/chat/completions` |
| 向量 Embedding | `http://127.0.0.1:8001/v1`，模型 `bge-m3` | **独立进程**；对话实例通常不提供 `/embeddings` |
| 向量维度 | `embedding.api.dimension: 1024` | 与 ES mapping `vector.dims` 一致 |

配置项见 `src/main/resources/application.yml`：`deepseek.api.*`、`embedding.api.*`、`rag.*`。

### 5.3 应用服务端口

| 服务 | 端口 |
| --- | --- |
| 后端 API | 8081 |
| 前端开发服 | 9527 |

---

## 6. 数据与索引规范

### 6.1 Elasticsearch 索引 `knowledge_base`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| fileMd5 | keyword | 文件指纹 |
| chunkId | integer | 分块序号 |
| parentId | keyword | 父块 ID |
| textContent | text (ik) | 检索正文 |
| parentTextContent | text | 父块全文（可不索引） |
| vector | dense_vector, **dims=1024** | 与 bge-m3 输出一致 |
| userId | keyword | 上传用户 DB ID |
| orgTag | keyword | 组织标签（注意与 `DEFAULT` 大小写兼容） |
| isPublic | boolean | 是否公开 |

### 6.2 索引写入成功条件

1. MinIO 桶 `uploads` 存在且分片合并成功；  
2. Kafka 消费完成解析，分块写入 `document_vectors`；  
3. Embedding API 返回 1024 维向量；  
4. ES bulk 无维度/映射错误。

**常见失败**：映射仍为 768 维、Embedding 服务未启动、MinIO `NoSuchBucket`。

---

## 7. 配置说明（RAG 与性能）

`application.yml` 中 `rag.pipeline` 建议：

| 配置项 | 本地开发建议 | 生产/高配 GPU 可选 |
| --- | --- | --- |
| enable-query-rewrite | `false` | `true` |
| enable-hyde | `false` | `true` |
| enable-reflection | `false` | `true` |

关闭上述开关后，问答检索主要依赖原始 query 的 BM25 + 向量双路召回，延迟显著降低。

---

## 8. 验收标准

> **开发/试点验收**（下表）。**生产上线**另见 [§11.4 生产验收门槛](#114-生产验收门槛)。

| 场景 | 验收条件 |
| --- | --- |
| 文件入库 | 上传 txt/pdf 后，Kafka 日志无持续报错；`curl ES/knowledge_base/_count` 大于 0 |
| 混合检索 | 登录后检索文档内关键词（如企业专有名词），返回带 `textContent` 的命中列表 |
| 智能问答 | 同一关键词提问，回答引用知识库内容或明确「暂无相关信息」 |
| 权限 | 非公开、非本组织文件不对无权限用户召回 |
| 健康检查 | `GET http://localhost:8081/` 返回 JSON 健康信息 |

---

## 9. 运维与排障

| 现象 | 可能原因 | 处理建议 |
| --- | --- | --- |
| 聊天搜不到已上传内容 | ES 文档数为 0 或检索反序列化失败 | 查 `knowledge_base/_count`、后端日志 `bulkIndex` / `Failed to decode`；确认维度 1024、重启后端触发重建 |
| 上传 500 MinIO | 桶不存在 | 确认 `uploads` 桶；或依赖 `MinioBucketInitializer` |
| 问答极慢 | Query 改写/HyDE 多次调用大模型 | 将 `rag.pipeline` 三项开关设为 `false` |
| 向量 404 | 仅启动对话 vLLM | 另起 8001 embedding 服务（bge-m3） |

应急脚本（可选）：`scripts/reindex-from-mysql.py` — 在 ES 映射已正确时，从 MySQL 分块重建索引。

完整安装步骤见 [操作手册.md](./操作手册.md)。

---

## 11. 生产环境需求（v1.2）

> 依据《test01 企业知识库项目评估与改进建议》（2026-06-03）及当前代码/运维现状整理。  
> 本章需求在 **试点验收（§8）** 之上叠加，**生产上线前须全部满足 P0 项**。

### 11.1 适用范围与结论

| 维度 | 说明 |
| --- | --- |
| 适用场景 | 小团队试点、部门级知识库、Web 端问答 |
| 不适用（未经改造） | 全公司直接上线、强敏感数据域、复杂组织权限、强依赖飞书入口 |
| 技术结论 | RAG 主链路完整，可作为试点基础；须完成安全、运维、评测与模型部署改造后再生产 |

**现网硬约束（须在部署方案中明确）：**

- 对话 LLM（8000）与向量 Embedding（8001）**须独立进程**；单卡 GPU 在 35B 级对话模型下通常**无法长期双开**，生产须采用：双卡部署、云端 Embedding、或运维切换策略（见 `NFR-PROD-07`）。
- Cross-Encoder：`rag.cross-encoder.api-url` 未配置时走 stub，不得默认认为精排已生效。

### 11.2 非功能需求（生产）

| 编号 | 类别 | 需求 | 优先级 |
| --- | --- | --- | --- |
| NFR-PROD-01 | 安全 | MySQL、Redis、MinIO、ES、JWT、模型 API Key 等敏感配置**仅通过环境变量 / Secret 注入**；代码库、镜像、Compose 文件不得含明文密钥 | P0 |
| NFR-PROD-02 | 安全 | **禁止**生产使用默认管理员 `admin/admin123`；首次部署强制初始化管理员并启用密码策略 | P0 |
| NFR-PROD-03 | 安全 | WebSocket 握手阶段校验 JWT；`/chat/**` 不得 `permitAll`；`allowedOrigins` 限制为生产前端域名白名单 | P0 |
| NFR-PROD-04 | 安全 | 禁止将 JWT **长期置于 URL 路径**；改为握手 Header 或短期 WS Ticket（建议有效期 ≤ 5 分钟） | P0 |
| NFR-PROD-05 | 安全 | WebSocket 与上传接口须配置连接频控、消息频控、单用户并发限制 | P1 |
| NFR-PROD-06 | 可用性 | LLM（8000）、Embedding（8001）独立健康检查；DOWN 时前台/监控页明确降级提示与处置建议 | P0 |
| NFR-PROD-07 | 可用性 | 文档化并落地 GPU/模型拓扑：双卡、云 Embedding 或分时切换；禁止未说明的「双服务同时在线」假设 | P0 |
| NFR-PROD-08 | 可用性 | MySQL、MinIO、Elasticsearch **定期备份**；每季度至少一次恢复演练 | P0 |
| NFR-PROD-09 | 可用性 | Kafka 消费积压、索引失败率、问答 P95 对接告警（邮件/企业 IM） | P0 |
| NFR-PROD-10 | 可维护性 | 日志集中采集（应用 + 中间件 + 模型服务）；保留审计日志不可篡改策略（至少 WORM/只读库） | P1 |
| NFR-PROD-11 | 性能 | 生产环境关闭 `rag.pipeline` 中 Query 改写/HyDE/Reflection（除非 GPU 与 SLA 已验证） | P1 |
| NFR-PROD-12 | 扩展 | 预留 `tenant_id`、`knowledge_base_id` 逻辑隔离字段，避免长期单索引混部 | P2 |

### 11.3 功能需求（生产）

#### 11.3.1 安全与 API

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| FR-PROD-SEC-01 | 文档预览、下载、删除等接口以 **`fileMd5` 或 `document_id`** 为主键；`fileName` 仅作展示，不得作为唯一定位键 | P0 |
| FR-PROD-SEC-02 | 管理员删除他人文档：先按 `fileMd5` 查询，再判断 owner 或 `ADMIN`，通过后再删除（修复「先按 userId 查导致 404」问题） | P0 |
| FR-PROD-SEC-03 | 上传：MIME 嗅探、扩展名白名单、单文件/总大小限制、Tika 解析超时、上传频控 | P0 |
| FR-PROD-SEC-04 | 可选：对接企业统一病毒扫描或沙箱解析服务 | P1 |

#### 11.3.2 RAG 质量与评测

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| FR-PROD-RAG-01 | 建立标准评测集：有标准答案题、无答案题、越权题，规模建议 **50～100** | P0 |
| FR-PROD-RAG-02 | 发版前自动执行评测：输出 TopK 召回率、答案正确率、引用准确率、越权次数 | P0 |
| FR-PROD-RAG-03 | Cross-Encoder：生产须**部署真实 reranker 并配置 `api-url`**，或文档明确关闭并关闭相关代码路径 | P1 |
| FR-PROD-RAG-04 | 回答须展示：引用来源、片段、文件名、命中分数；低置信度或无检索时**强制拒答**（对齐 FR-CHAT-05） | P0 |
| FR-PROD-RAG-05 | 支持用户反馈：有用/无用/引用错误/答案错误/缺文档，并纳入运营统计 | P1 |

#### 11.3.3 知识治理

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| FR-PROD-GOV-01 | 文档元数据：负责人、所属部门、生效时间、失效时间、密级、是否允许 AI 引用 | P1 |
| FR-PROD-GOV-02 | 已废止/未发布文档不参与检索 | P1 |
| FR-PROD-GOV-03 | 同一问题命中多版本/冲突数值时，提示以**最新生效**文件为准 | P2 |

#### 11.3.4 运维与管理端

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| FR-PROD-OPS-01 | 运行监控展示各组件 UP/DOWN；向量/对话 DOWN 时展示**处置建议**（与手工启动指南一致） | P0 |
| FR-PROD-OPS-02 | 索引失败：可重试、可观测；Kafka DLT/积压有人工处理流程 | P0 |
| FR-PROD-OPS-03 | 运营看板：高频无答案问题、索引失败文档、低命中文档、差评答案（P1 可先做统计表） | P1 |

#### 11.3.5 权限扩展（试点后可分期）

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| FR-PROD-AUTH-01 | 在 `org_tags` 基础上扩展：部门、项目组、文档密级、临时授权、授权有效期（试点至少实现 **密级 + 有效期** 之一） | P1 |
| FR-PROD-AUTH-02 | 答案生成后二次审查：不得泄露无权限文件名/片段/敏感字段 | P2 |

#### 11.3.6 渠道与架构（规模化阶段）

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| FR-PROD-ARCH-01 | 抽取统一 `RagAnswerService`，Web / 开放 API / 飞书共用同一检索与生成逻辑 | P2 |
| FR-PROD-FEISHU-01 | 飞书：事件订阅、签名校验、用户绑定、会话映射、消息去重、卡片式回复 | P2 |
| FR-PROD-FAQ-01 | 高频 FAQ 优先返回标准答案，RAG 作为补充 | P2 |

### 11.4 生产验收门槛

在 **§8 验收标准** 全部通过的前提下，生产上线还须满足：

| 指标 | 门槛 |
| --- | --- |
| 权限越权（越权评测集） | **0 次** |
| Top5 召回率 | ≥ **85%** |
| 有答案问题正确率 | ≥ **80%** |
| 无答案问题拒答率 | ≥ **90%** |
| 引用来源准确率 | ≥ **85%** |
| 问答 P95（关闭 Query 改写/HyDE，试点库规模） | ≤ **10 秒** |
| 索引失败 | 可观测、可重试；24h 内处理率 **100%** |
| 默认密码/明文密钥 | 生产环境抽检为 **0** |
| 模型服务 SLA | 对话、向量服务状态可监控；允许分时部署但须有**切换/runbook** |

### 11.5 实施阶段建议

| 阶段 | 周期（参考） | 目标 | 关键交付 |
| --- | --- | --- | --- |
| **阶段 0：生产基线** | 2～4 周 | 安全与可运维 | Secret 外置、WS 安全、fileMd5 API、删除修复、上传加固、备份告警、GPU/Embedding 方案 |
| **阶段 1：试点可运营** | 4～8 周 | 效果可验证 | **1～2 个业务知识库**（orgTag 配置）、100～300 份/库、分库评测集（[eval/](./eval/README.md)）、反馈闭环 |
| **阶段 2：规模化** | 8 周+ | 多渠道与多库 | 飞书机器人、多知识库隔离、FAQ、HA 部署（K8s/托管 ES） |

### 11.6 目标架构（推荐）

Web、飞书、开放 API 不应各写一套问答逻辑，推荐统一为：

```text
Web / Feishu / Open API
        ↓
Auth & User Mapping
        ↓
Conversation Service
        ↓
RAG Answer Service（统一）
        ↓
Retrieval Pipeline
        ↓
LLM Client
        ↓
Citation Formatter
```

### 11.7 相关文档

- 评估原文：`test01企业知识库项目评估与改进建议.md`（桌面/项目评估输入）
- [知识库分域与边界定义.md](./知识库分域与边界定义.md)（业务库划分、权限矩阵、试点节奏）
- [eval/README.md](./eval/README.md)（分库评测集 JSONL 规范）
- [功能增强变更说明.md](./功能增强变更说明.md)（已实现能力追踪）
- [手工启动指南.md](./手工启动指南.md)（8000/8001 启动与验收）
- [操作手册.md](./操作手册.md)（安装部署与排障）

---

## 10. 文档修订记录

| 版本 | 日期 | 变更摘要 |
| --- | --- | --- |
| v1.0 | — | 初版（随 README 项目说明） |
| v1.1 | 2026-05-20 | 补充 vLLM 双服务、ES 1024 维、bge-m3、RAG 开关、MinIO/索引排障与验收标准 |
| v1.2 | 2026-05-20 | 新增 [操作手册.md](./操作手册.md) 安装部署指南 |
| v1.3 | 2026-06-03 | 新增 [§11 生产环境需求（v1.2）](#11-生产环境需求v12)：安全、运维、评测、GPU 约束、验收门槛与分阶段路线 |
| v1.4 | 2026-06-03 | 新增 [§3.6 知识库分域（试点）](#36-知识库分域试点)、[§3.7 多知识库产品化](#37-多知识库产品化阶段-2)；阶段 1 交付物明确 1～2 业务库 |
